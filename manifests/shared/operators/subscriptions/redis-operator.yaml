apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: redis-operator
  namespace: operators # Assuming operators are installed in 'operators' namespace
spec:
  channel: stable # Placeholder, please verify
  installPlanApproval: Automatic
  name: redis-operator # Placeholder for a community Redis operator, please verify
  source: community-operators # Or 'redhat-operators' if from Redis Ltd. via Marketplace
  sourceNamespace: olm
